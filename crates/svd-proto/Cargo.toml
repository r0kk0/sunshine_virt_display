[package]
name = "svd-proto"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Shared types for sunshine-virt-display (no I/O)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
