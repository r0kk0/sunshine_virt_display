[package]
name = "svd-proto"
version = "0.1.0"
edition = "2021"
description = "Shared types for sunshine-virt-display (no I/O)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
